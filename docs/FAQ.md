# FAQ

## Is this repo release-ready?

No. The current verdict is `INCONCLUSIVE`.

## Why is it inconclusive?

Because the primary quality scorecard passes while the handoff manifest still says `NO-GO`.

## What actually works now?

The Python codec package installs, the local pytest surface passes, and curated Wave-1 proof anchors are preserved in-repo.

## Why is the raw artifact warehouse not inside this repo?

Because it is large, noisy, and operator-oriented. This repo keeps only the proof subset needed for future verification.

## Where is the format contract?

`docs/family/ZPINK_INTERFACE_CONTRACT.md`
