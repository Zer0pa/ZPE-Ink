# Research State

## Project Reference

See: .gpd/PROJECT.md

**Core research question:** Can ZPE-Digital-Ink become a contradiction-free, same-corpus-honest, commercially disruptive ink utility kernel, or must the claim stay narrowly scoped to structured-tier transport plus explicit candidate branches?
**Current focus:** Phase 05.1 is complete. The commercial verdict remains `NO-GO`, but a bounded interoperability candidate lane is now reopened on a coherent repo truth surface with a candidate-only token sidecar.

## Current Position

**Current Phase:** 05.1
**Current Phase Name:** Interoperability Truth Reconciliation and Bounded Augmentation
**Total Phases:** 6
**Current Plan:** 1
**Total Plans in Phase:** 1
**Status:** Complete
**Last Activity:** 2026-04-24

**Progress:** [██████████] 100%

## Active Calculations

None yet.

## Intermediate Results

- The sovereign release surface is still `FAIL` / `NO-GO`; the current repo truth surface is now coherent.
- Frozen sovereign structured-tier ratio remains `5.590209480060199x` against the raw float32 baseline.
- The Phase 04 primitive-token candidate beats the frozen structured-tier `brotli` surface on ratio:
  - `primitive-token + zstd`: `12.377589251348581x`
  - `brotli`: `6.825565026256283x`
  - `sovereign zpe_ink`: `5.590209480060199x`
- The same primitive-token candidate is not promotable:
  - bounded Calliar ratio: `2.883090351936113x`
  - bounded Calliar mean Hausdorff: `201.11119574321816 px`
  - bounded Calliar max Hausdorff: `636.5736406732532 px`
- A real non-Latin online-stroke corpus now exists in-lane:
  - Calliar: `2.774608127006351x` over `2500` Arabic online-stroke samples
- The tokenizer scaffold is now real:
  - QuickDraw `cat` proof slice: `100` samples, `926` strokes, deterministic `8`-token vocabulary
- Phase 05 converted the 2026-04-05 action brief into a machine-readable reality audit instead of treating it as proof.
- Phase 05 wedge ranking result:
  - current wedge verdict: `NO_CURRENT_WEDGE_PROVEN`
  - narrowest future candidate: `interoperability`
- Phase 05 final decision surface:
  - enterprise readiness: `NOT_READY`
  - final go/no-go: `NO-GO`
- Phase 05.1 follow-on decision surface:
  - commercial readiness: `NO-GO`
  - follow-on lane: `OPEN_BOUNDED_CANDIDATE`
  - candidate token sidecar: exact on directional/proxy fixtures, not promotable on adversarial high-velocity fixtures
- Canonical telemetry constants remain:
  - Comet workspace/project: `zer0pa / zpe-ink`
  - Opik: no canonical surface found in this workstream
  - Local telemetry execution stayed skipped because `COMET_API_KEY` is absent in this shell
- IAM landing page is reachable (`HTTP 200`), while UNIPEN remains unresolved at DNS level.
- Current verified free disk is roughly `22 GiB` local in the current shell.

## Open Questions

- Should the bounded interoperability lane stay local-first until blind-clone and hard-corpus gates justify more?
- Does any future token-sidecar work need a public corpus beyond the current directional/proxy fit surface?
- When should blind-clone and telemetry reruns be reopened relative to the bounded interoperability lane?

## Performance Metrics

| Label | Duration | Tasks | Files |
| ----- | -------- | ----- | ----- |
| Phase 02 closeout | complete | contradiction classification, comparator freeze, honest claim-scope update | `.gpd/`, `ZPE-Ink/code/`, `ZPE-Ink/proofs/reruns/` |
| Phase 03 external closure | complete | RunPod blind clone, IAM/UNIPEN boundary probe, Calliar benchmark, roadmap coherence repair | `.gpd/`, `ZPE-Ink/code/`, `ZPE-Ink/proofs/reruns/phase3_external/` |
| Phase 04 candidate branches | complete | primitive-token runtime benchmark, tokenizer scaffold proof, phase sidecar truth surface | `.gpd/`, `ZPE-Ink/code/`, `ZPE-Ink/proofs/reruns/primitive_token_branch/`, `ZPE-Ink/proofs/reruns/tokenizer_scaffold/`, `ZPE-Ink/proofs/reruns/phase4_runtime/` |
| Phase 05 wedge proof | complete | action-brief reality audit, wedge candidate matrix, enterprise boundary note, final go/no-go closure | `.gpd/`, `ZPE-Ink/proofs/reruns/phase5_wedge/` |
| Phase 05.1 bounded follow-on | complete | truth reconciliation, local CPU rerun refresh, bounded interoperability verdict, candidate token-sidecar proof | `.gpd/`, `ZPE-Ink/code/`, `ZPE-Ink/docs/`, `ZPE-Ink/proofs/reruns/follow_on_reassessment_2026-04-24/` |

## Accumulated Context

### Decisions

- Treat the existing `.gpd` workspace as a live project and re-scope in place instead of rerunning project bootstrap.
- Make contradiction resolution and comparator freeze the current gate; later wedge or branch work stays subordinate.
- Preserve the structured-tier-only qualifier and the active `LicenseRef-Zer0pa-SAL-6.0` surface as hard constraints.
- Use the local M1 lane first; Red Magic only when attached and RunPod only after explicit user approval.
- Treat frozen engineering-comparator results as sovereign against any broader “commercially disruptive compression” language; `brotli` currently beats `zpe_ink` on the structured tier.
- Use RunPod direct TCP as the authoritative Phase 03 external host because the pod alias path is not currently key-valid.
- Treat Calliar as the strongest honest non-Latin online-stroke surface currently available in-lane.
- Treat blind-clone core success as valuable but insufficient for a release `GO`.
- Keep the primitive-token runtime candidate-only even after its structured-tier `brotli` win, because bounded Calliar fidelity collapses.
- Treat the tokenizer scaffold as shipped evidence of branch viability, not as wedge or release closure.
- Treat the 2026-04-05 action brief as candidate input rather than contract override when it conflicts with the live repo state.
- Close Phase 05 with a final `NO-GO`: no current wedge is proven; interoperability is only a future candidate lane on a stabilized repo state.
- Close Phase 05.1 with a bounded update: commercial `NO-GO` remains, but the interoperability lane is reopened as `OPEN_BOUNDED_CANDIDATE` on a coherent local proof surface.

### Active Approximations

None yet.

**Convention Lock:**

No conventions locked yet.

### Propagated Uncertainties

None yet.

### Pending Todos

- Decide whether the bounded interoperability lane should remain local-only until blind-clone and hard-corpus gates improve.
- Decide whether future candidate token-surface work should target retrieval utility or stay strictly as interchange indexing.
- Restore `COMET_API_KEY` and `RUNPOD_API_KEY` only if a future lane truly requires telemetry or external reruns.

### Blockers/Concerns

- Sovereign release verdict remains `FAIL` / `NO-GO`.
- Hard-corpus authority remains weak and blocks broad compression claims.
- The sovereign runtime still loses to `brotli`; the primitive-token branch beats `brotli` only as a lossy candidate and is not promotable.
- Interoperability is now a bounded follow-on candidate, not a current wedge.
- The token sidecar degrades on adversarial high-velocity fixtures and cannot be promoted as a general runtime.
- Direct UNIPEN closure remains unresolved even after the external phase.
- Local `COMET_API_KEY` and `RUNPOD_API_KEY` are absent, so telemetry and external reruns did not execute in this shell.

## Session Continuity

**Last session:** 2026-04-24
**Stopped at:** Phase 05.1 complete with a coherent truth surface, passing local CPU rerun, and a bounded interoperability candidate lane.
**Resume file:** `.gpd/phases/05.1-interoperability-truth-reconciliation-and-bounded-augmentation/05.1-01-SUMMARY.md`
