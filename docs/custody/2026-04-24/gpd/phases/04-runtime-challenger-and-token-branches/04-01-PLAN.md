---
phase: 04-runtime-challenger-and-token-branches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ZPE-Ink/code/zpe_ink/primitivetoken.py
  - ZPE-Ink/code/tests/test_primitivetoken.py
  - ZPE-Ink/code/scripts/run_primitivetoken_benchmark.py
  - ZPE-Ink/proofs/reruns/primitive_token_branch/primitivetoken_benchmark.json
  - .gpd/phases/04-runtime-challenger-and-token-branches/04-01-SUMMARY.md
interactive: false

conventions:
  units: "integer coordinate samples"
  metric: "same-corpus byte ratio"
  coordinates: "screen Cartesian"

dimensional_check:
  compression_ratio: "[dimensionless]"
  hausdorff_px: "[px]"

approximations:
  - name: "8-direction primitive quantization"
    parameter: "direction alphabet size = 8"
    validity: "candidate-only branch comparison against structured-tier and bounded cross-script corpora"
    breaks_when: "trajectory fidelity error or side-channel handling makes the candidate unusable"
    check: "report Hausdorff error and preserve side channels exactly"
  - name: "fixed-step reconstruction"
    parameter: "step_size"
    validity: "lossy branch benchmarking only"
    breaks_when: "step_size hides trajectory distortion or inflates the ratio unfairly"
    check: "measure reconstruction error explicitly"

contract:
  schema_version: 1
  scope:
    question: Can a candidate primitive-token runtime close any part of the structured-tier `brotli` gap or improve bounded cross-script behavior without being mistaken for the sovereign runtime?
  claims:
    - id: claim-brch01-candidate
      statement: Phase 04 can honestly evaluate a primitive-token runtime only by implementing it as a candidate branch, testing it, benchmarking it against the frozen sovereign codec and `brotli`, and publishing the result without promotion.
      deliverables: [deliv-primitive-module, deliv-primitive-tests, deliv-primitive-benchmark, deliv-summary]
      acceptance_tests: [test-primitive-tests, test-primitive-benchmark, test-summary]
      references: [ref-prd, ref-freeze, ref-claim-scope, ref-calliar]
  deliverables:
    - id: deliv-primitive-module
      kind: code
      path: ZPE-Ink/code/zpe_ink/primitivetoken.py
      description: Candidate primitive-token encoder/decoder with pack/unpack support.
    - id: deliv-primitive-tests
      kind: code
      path: ZPE-Ink/code/tests/test_primitivetoken.py
      description: Unit tests for direction mapping, packing, determinism, fidelity, and side-channel preservation.
    - id: deliv-primitive-benchmark
      kind: report
      path: ZPE-Ink/proofs/reruns/primitive_token_branch/primitivetoken_benchmark.json
      description: Honest benchmark comparing the candidate branch to the sovereign codec and `brotli`.
    - id: deliv-summary
      kind: report
      path: .gpd/phases/04-runtime-challenger-and-token-branches/04-01-SUMMARY.md
      description: Plan-01 summary with candidate-only interpretation.
  references:
    - id: ref-prd
      kind: prior_artifact
      locator: ZPE-Ink/PRD_ZPE_DIGITAL_INK.md
      role: definition
      why_it_matters: Preserves the anti-theatre candidate-branch and authority-metric rules.
      applies_to: [claim-brch01-candidate]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-freeze
      kind: prior_artifact
      locator: ZPE-Ink/proofs/reruns/benchmark_freeze_local/baseline_results.json
      role: benchmark
      why_it_matters: Provides the frozen sovereign codec and comparator ratios.
      applies_to: [claim-brch01-candidate]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-claim-scope
      kind: prior_artifact
      locator: ZPE-Ink/proofs/reruns/benchmark_freeze_local/claim_scope_map.json
      role: definition
      why_it_matters: Preserves the current allowed and blocked claim surface.
      applies_to: [claim-brch01-candidate]
      must_surface: true
      required_actions: [read, compare, cite]
    - id: ref-calliar
      kind: prior_artifact
      locator: ZPE-Ink/proofs/reruns/phase3_external/calliar_benchmark.json
      role: benchmark
      why_it_matters: Gives the bounded non-Latin comparison surface that must stay separate from structured-tier claims.
      applies_to: [claim-brch01-candidate]
      must_surface: true
      required_actions: [read, compare, cite]
  acceptance_tests:
    - id: test-primitive-tests
      subject: claim-brch01-candidate
      kind: reproducibility
      procedure: Run the new primitive-token unit tests under the repo test environment.
      pass_condition: The test file passes and the branch stays deterministic.
      evidence_required: [deliv-primitive-module, deliv-primitive-tests]
      automation: automated
    - id: test-primitive-benchmark
      subject: claim-brch01-candidate
      kind: benchmark
      procedure: Benchmark the primitive-token candidate against the sovereign codec and `brotli` on structured fixtures and a bounded Calliar sample.
      pass_condition: The benchmark artifact reports all ratios and fidelity costs explicitly; any result may pass so long as it is honest and candidate-only.
      evidence_required: [deliv-primitive-benchmark, ref-freeze, ref-calliar]
      automation: automated
    - id: test-summary
      subject: claim-brch01-candidate
      kind: human_review
      procedure: Read the benchmark artifact and write a bounded summary.
      pass_condition: The summary states whether the branch beat `brotli` without promoting it into sovereign runtime truth.
      evidence_required: [deliv-primitive-benchmark, deliv-summary]
      automation: hybrid
  forbidden_proxies:
    - id: fp-primitive-promotion
      subject: claim-brch01-candidate
      proxy: Treating any candidate-branch gain as a sovereign-runtime win.
      reason: Phase 04 is allowed to explore candidate branches, not to rewrite the frozen authority surface by narration.
    - id: fp-ratio-only
      subject: claim-brch01-candidate
      proxy: Reporting candidate compression gains without fidelity accounting.
      reason: A smaller payload is not meaningful if the branch silently destroys trajectory truth.
---

<objective>
Implement and benchmark a candidate primitive-token runtime branch against the frozen sovereign codec and `brotli`.
</objective>

<context>
@.gpd/PROJECT.md
@.gpd/STATE.md
@.gpd/phases/04-runtime-challenger-and-token-branches/04-RESEARCH.md
@ZPE-Ink/PRD_ZPE_DIGITAL_INK.md
@ZPE-Ink/proofs/reruns/benchmark_freeze_local/baseline_results.json
@ZPE-Ink/proofs/reruns/benchmark_freeze_local/claim_scope_map.json
@ZPE-Ink/proofs/reruns/phase3_external/calliar_benchmark.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the primitive-token candidate module</name>
  <files>ZPE-Ink/code/zpe_ink/primitivetoken.py</files>
  <action>Create a candidate-only 8-direction primitive-token encoder/decoder with explicit pack and unpack helpers and exact side-channel carry-through.</action>
  <verify>Keep the candidate separate from the sovereign codec and make the token representation deterministic for identical inputs.</verify>
  <done>The module exposes a usable candidate branch surface without replacing the sovereign codec.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic primitive-token tests</name>
  <files>ZPE-Ink/code/tests/test_primitivetoken.py</files>
  <action>Write tests for direction mapping, pack/unpack reversibility, side-channel preservation, deterministic output, and bounded lossy reconstruction error.</action>
  <verify>Run the primitive-token tests under pytest and fail on drift or hidden randomness.</verify>
  <done>The candidate runtime has an executable correctness surface.</done>
</task>

<task type="auto">
  <name>Task 3: Benchmark the candidate honestly</name>
  <files>ZPE-Ink/code/scripts/run_primitivetoken_benchmark.py, ZPE-Ink/proofs/reruns/primitive_token_branch/primitivetoken_benchmark.json</files>
  <action>Measure the sovereign codec, primitive-token raw, primitive-token plus `zstd`, and `brotli` on the structured fixtures and a bounded Calliar sample with exact same-corpus byte accounting.</action>
  <verify>Report the candidate's fidelity cost explicitly and keep the structured-tier and Calliar surfaces separate.</verify>
  <done>A benchmark artifact exists that can either support or falsify the candidate branch honestly.</done>
</task>

<task type="auto">
  <name>Task 4: Publish the Phase 04 plan-01 summary</name>
  <files>.gpd/phases/04-runtime-challenger-and-token-branches/04-01-SUMMARY.md</files>
  <action>Write a bounded summary of what the candidate runtime did or did not achieve.</action>
  <verify>Do not promote the candidate branch into sovereign runtime identity or release language.</verify>
  <done>The branch result is explicit and ready for Phase 04 carry-forward.</done>
</task>

</tasks>

<verification>
Run the new primitive-token pytest surface, verify the benchmark JSON parses cleanly, and check that any candidate-branch claim stays subordinate to the frozen Phase 02 authority surface.
</verification>

<success_criteria>
The primitive-token candidate exists, is tested, is benchmarked against the frozen sovereign runtime and `brotli`, and has one honest summary that preserves candidate-only language.
</success_criteria>

<output>
After completion, create `.gpd/phases/04-runtime-challenger-and-token-branches/04-01-SUMMARY.md`.
</output>
