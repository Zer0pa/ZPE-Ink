# Phase 05 Wedge Proof Memo

## Final Wedge Verdict

No current commercial wedge is proven.

Phase 05 does not end in a narrowed `GO`; it ends in a truthful `NO-GO` with one future candidate lane named explicitly: interoperability.

## Why Transport Does Not Survive

The sovereign transport kernel still preserves a structured-tier pass at `5.590209480060199x`, but that is not enough to survive as the commercial wedge:

- the frozen engineering comparator verdict is still negative because `brotli` remains stronger at `6.825565026256283x`
- hard-corpus authority remains weak (`MathWriting=1.0944074088858728x`, `CROHME=1.301456280301924x`)
- the contradiction surface remains open and the sovereign release surface remains `FAIL` / `NO-GO`

Transport therefore survives only as a narrow truth surface, not as enterprise-ready product proof.

## Why Tokenizer Does Not Survive

The tokenizer branch remains a scaffold proof only. Phase 04 established bounded determinism and corpus proof for a small QuickDraw slice, but nothing in that branch promotes it to a market-ready wedge. Treating it as such would convert a candidate scaffold into false closure.

## Why Interoperability Is the Narrowest Future Candidate

The 2026-04-05 action brief is strongest where it proposes Swift/C# decoder completion plus cross-runtime parity. That cluster maps to a narrow and intelligible product family: a cross-runtime ink interchange kit. This lane is more plausible than transport or tokenizer because it does not require pretending the sovereign runtime wins on the hard corpora or that the tokenizer scaffold is already promotable.

It still does not survive today.

Current blockers:

- the local worktree deletes the tracked Swift, C#, and core codec files that the brief wants to extend
- the `HEAD` versions of Swift and C# are still header-level probes, not full decoder parity implementations
- an honest proof pass would require stabilizing the repo state and rerunning binding-contract plus cross-runtime checks on real decoder surfaces

So interoperability is the narrowest future candidate, but not a current wedge.

## Enterprise Readiness

Enterprise readiness does not survive Phase 05.

The blocking evidence is cumulative:

- contradiction surface still open
- sovereign release still `NO-GO`
- blind clone still `INCONCLUSIVE`
- hard-corpus weakness remains explicit
- current worktree diverges from the tracked runtime and binding surfaces
- active license remains `LicenseRef-Zer0pa-SAL-6.0`

The honest enterprise posture is therefore:

- `NOT_READY`
- `NO-GO`
- candidate follow-on permitted only as a narrow interoperability rehabilitation lane, not as a retroactive upgrade of the current repo

## Follow-on Lane

If follow-on work is approved after this phase, the best next lane is:

1. stabilize or explicitly restore the deleted binding surfaces on a clean repo state
2. implement real Swift and C# decoders
3. rerun binding-contract and cross-runtime parity checks
4. re-evaluate whether interoperability has become a narrow candidate wedge

That is future branch work. It is not Phase 05 closure.
